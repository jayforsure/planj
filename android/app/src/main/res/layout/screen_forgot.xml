<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingHorizontal="24dp"
    android:paddingTop="8dp"
    android:paddingBottom="32dp">

        <com.planj.phone.Illustration
            style="@style/Auth.Illustration"
            android:contentDescription="small, about 5:2"
            android:tag="forgot" />

        <TextView style="@style/Auth.Title" android:text="Forgot your password?" />
        <TextView android:id="@+id/blurb" style="@style/Auth.Blurb" android:text="Enter your email and we will send a code to set a new one." />
        <TextView android:id="@+id/error" style="@style/Auth.Error" />
        <TextView style="@style/Auth.Label" android:text="Email" />
        <EditText
            android:id="@+id/email"
                style="@style/Auth.Field"
            android:layout_marginTop="6dp"
            android:hint="you@example.com"
            android:imeOptions="actionDone"
            android:inputType="textEmailAddress" />
        <TextView android:id="@+id/email_hint" style="@style/Auth.Hint" />
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">
            <Button android:id="@+id/btn_submit" style="@style/Auth.Primary" android:text="Send reset code" />
            <ProgressBar android:id="@+id/progress" android:layout_width="22dp" android:layout_height="22dp" android:layout_gravity="center" android:layout_marginTop="20dp" android:indeterminateTint="@color/on_accent" android:visibility="gone" />
        </FrameLayout>
        <TextView android:layout_width="match_parent" android:layout_height="wrap_content" android:layout_marginTop="12dp" android:lineSpacingExtra="3dp" android:text="Your password also locks your data key. After resetting, you will need your recovery code to keep your existing synced data." android:textColor="@color/warn" android:textSize="12sp" />
</LinearLayout>
