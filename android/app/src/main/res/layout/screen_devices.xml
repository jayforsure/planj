<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingHorizontal="24dp"
    android:paddingTop="8dp"
    android:paddingBottom="32dp">

        <TextView style="@style/Auth.Title" android:text="Devices" />
        <TextView android:id="@+id/blurb" style="@style/Auth.Blurb" android:text="Every device signed in to this account. Removing one signs it out; its own recorded data stays on it." />
        <TextView android:id="@+id/error" style="@style/Auth.Error" />
        <LinearLayout
            android:id="@+id/list"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:orientation="vertical" />
        <ProgressBar android:id="@+id/progress" android:layout_width="22dp" android:layout_height="22dp" android:layout_gravity="center_horizontal" android:layout_marginTop="24dp" android:indeterminateTint="@color/accent" />
</LinearLayout>
