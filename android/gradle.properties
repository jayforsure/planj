# Tuned for a low-memory machine (WSL has ~3 GB shared with VS Code).
org.gradle.jvmargs=-Xmx640m -XX:MaxMetaspaceSize=320m -XX:+UseSerialGC -Dfile.encoding=UTF-8
org.gradle.daemon=false
org.gradle.workers.max=1
org.gradle.parallel=false
kotlin.compiler.execution.strategy=in-process
